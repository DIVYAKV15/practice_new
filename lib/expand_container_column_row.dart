import 'package:flutter/material.dart';

void main()
{
  runApp(MaterialApp(home:Practice()),);

}
class Practice extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(Title(color: color, child: child)
      ),
    );
  }

}